import { DefaultToastOptions, ToastOptions } from "react-hot-toast";

const toasterConfig: DefaultToastOptions = {
  position: 'bottom-right',
  style: {
    border: '1px solid #713200',
    padding: '10px',
  },
};
export default toasterConfig;